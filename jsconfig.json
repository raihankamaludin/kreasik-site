{
  "compilerOptions": {
    // ...
    "paths": {
      // A new catch-all that replaces the baseUrl:
      "*": ["."],

      // Every other path now has an explicit common prefix:
      "@/*": ["./src/*"],
      
    }
  }
}